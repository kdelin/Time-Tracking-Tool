// Define the Packagepackage classes.view.picklist {		// Import Necessary Classes	import flash.filters.DropShadowFilter;	import flash.text.TextField;	import flash.text.AntiAliasType;	import flash.text.TextFormat;	import flash.text.TextFieldType;	import flash.events.Event;	import flash.utils.setTimeout;	import flash.events.TimerEvent;	import flash.utils.Timer;		import flash.events.KeyboardEvent;		import classes.view.picklist.pickListEvent;		// Define the Class	internal class PLInputHint extends TextField {						public function PLInputHint( obj:Object = null ):void {									obj = obj || new Object( );						// Set the Text Format			defaultTextFormat = new TextFormat ( 'Arial', 13, 0xCCCCCC, null, null, null, null, null, null, 20 );						// Set Format properties			antiAliasType = AntiAliasType.ADVANCED;			type = TextFieldType.INPUT;			multiline = false;			doubleClickEnabled = true;			embedFonts = true;									width = obj.width || 200;			height = obj.width || 30;			y = obj.y || 13;						// Initialize/ReInitialize Properties			init ( obj );		}				// Initialize/ReInitialize Properties		public function init ( obj:Object ) : void {			for ( var n:String in obj ) if ( n in this ) this [ n ] = obj [ n ];		}				// Override the Text Property		public override function set text ( val:String ) : void {			// Only Change Text when Necessary			if ( ( val || val === '' ) && val != text ) super.text = val;		}	}}