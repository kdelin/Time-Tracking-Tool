// Define the Packagepackage classes.view.grid {		// Import Necessary Classes	import flash.filters.DropShadowFilter;	import flash.display.DisplayObject;	import flash.filters.GlowFilter;	import flash.events.Event;		// Define the Class	public final class GridRow extends GridSprite {				// Crate a Vector for GridText Objects		public var gridtext:Vector.<GridText> = new <GridText> [ ];		// Create a Variable for Column Index		public var index:uint = 0;				// Instantiate the Class		public function GridRow ( obj:Object ) : void {			// Add Content			for each ( var i:DisplayObject in obj.content ) addChild ( i );			// Create DropShadowFilter Object			filters = filters.concat ( new DropShadowFilter ( 5, 90, 0xFFFFFF, 1, 0, 5, .7, 3, true, false, false ) );			// Create GlowFilter Object			filters = filters.concat ( new GlowFilter ( 0, 1, 0, 3, 1, 1, true, false ) );			// Allow Double Clicks			doubleClickEnabled = true;			// Set Event Listeners			GridListener.set ( { target:this, doubleClick:on_double, blur:on_blur, focus:on_focus } );			// Reset the Scale			scaleX = scaleY = 1;			// Define a Fill Color			graphics.beginFill ( 0, 0 );			// Draw a Rectangle			graphics.drawRect ( -25, 0, ( obj.width || 780 ) + 25, obj.height || 45 );			// Initialize/Reinitialize Properties			init ( obj );			//			on_blur ( null );		}				// Initialize/Reinitialize Properties		public function init ( obj:Object ) : void {			// Exclude Object Properties			for ( var i:String in [ 'width', 'height' ] ) delete obj [ n ];			// Initialize/Reinitialize Properties			for ( var n:String in obj ) if ( n in this ) this [ n ] = obj [ n ];		}				// 		private function on_focus ( evt:Event ) : void {			//			opaqueBackground = 0xEEEEEE;		}				// 		private function on_blur ( evt:Event ) : void {			//			opaqueBackground = 0xEAEAEA;		}				// 		private function on_double ( evt:Event ) : void {			// Update the TextField			gridtext [ 0 ].init ( { mouseEnabled:true } );			// Set the Focus			stage.focus = gridtext [ 0 ];		}	}}