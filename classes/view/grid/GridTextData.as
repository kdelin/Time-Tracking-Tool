// Define the Packagepackage classes.view.grid {		// Import Necessary Classes	import flash.filters.GlowFilter;	import flash.geom.Rectangle;	import flash.geom.Point;	import flash.globalization.DateTimeFormatter;	import flash.globalization.LocaleID;		// Define the Class	internal final class GridDataManager {				// 		private var columnInfo:Vector.<Object> = new <Object> [ ];		// 		private var rowInfo:Vector.<Object> = new <Object> [ ];		// 		private var rowPos:Vector.<Object> = new <Object> [ ];		// 		internal var columnValues:Vector.<String> = new <String> [ ];		// 		internal var rowValues:Vector.<Vector.<String>>;		// 		internal var columnCount:int = 0;		// 		internal var rowCount:int = 0;				// Instantiate the Class		public function GridDataManager ( obj:Object = null ) : void {			// Initialize Properties			init ( obj );		}				// Initialize/Reinitialize the Class		public function init ( obj:Object ) : void {			//			if ( !obj ) return;			// 			columnInfo = Vector.<Object> ( obj.columnInfo || columnInfo );			// 			rowInfo = Vector.<Object> ( obj.rowInfo || rowInfo );			//			columnCount = columnInfo.length;			//			rowCount = rowInfo.length;			//			columnValues = new Vector.<String> ( columnCount );			//			rowValues = new Vector.<Vector.<String>> ( rowCount );			//			for ( var n:int = columnCount; n--; ) {				//				var nfo:Object = columnInfo [ n ];				//				var tot:Number = nfo.x = tot;				//				tot + ( nfo.width || 156 );				//				nfo.fnc = this [ nfo.type || 'text' ] ( nfo.list );				//				columnValues [ n ] = nfo.label;				//				for ( var i:int = rowCount; i--; ) {					//					rowValues [ i ] = rowValues [ i ] || new Vector.<String> ( columnCount );					//					rowValues [ i ] [ n ] = rowInfo [ i ][ n ];					//					trace ( rowValues [ i ] );					//					nfo.row = Vector.<String> ( obj.rowInfo [ n ] );				}			}					}				//		private function text ( arr:Array = null ) : Function {			//			return function ( val:String ) : String {				//				return val;			}		}								//		private function list ( arr:Array ) : Function {			//			var list:Vector.<String> = Vector.<String> ( arr );			//			return function ( ndx:int ) : String {				//				return list [ ndx ];			}		}				//		private function date ( arr:Array = null ) : Function {			//			var fmt:DateTimeFormatter = new DateTimeFormatter ( LocaleID.DEFAULT );			//			fmt.setDateTimePattern ( 'MM/dd/yy hh:mm a z' );			//			return function ( mil:int ) : String {				//				return fmt.format ( new Date ( mil ) );			}		}								//		private function hours ( arr:Array = null ) : Function {			//			return function ( num:Number ) : String {				//				return [ int ( num ), ( num - int ( num ) ) * 60 ].join ( ':' );			}		}					}}